################################################
                INITIAL SETUP
################################################

1) Set up setup.sql and dummy.sql files [X]
2) Create MVC Structure with resource folder [X]
3) Set up controller, routes and server information [X]
4) Set up Database for local testing [X]
5) Set up CSS, JS & Framework files [X]
6) Set up a template.html file for later use [X]
7) Private the Github Repository [X]
8) Consult and find solution for EAN scanner [X]
9) Get a clear vision and a pathing for it [X]
10) Set up Entity, DTO & Relevant Mappers [X]
11) Set up Pathing Config to avoid problems with production [X]
12) Set up module (components) .js file folder [X]

################################################
              STEPS TO KEEP TRACK
################################################

1) Create index.html | Scanner []
2) Create product.html | Simple title render first []
3) Skip categories and misc stuff for now. Will mainly work on search []
4) Set up setup.sql & dummy.sql with relevant backend information []
5) Set up mapper / search feature (loads the products page) []
6) No product found -> We're just launched. Adding products slowly (popup) | Request quick add []
7) Set up user product request in db (requested_products). No need for frontend there for now []
8) We will then take the product and add those faster than the rest []
9) Allow users to rate a product []
10) rate.html setup (rate a product step-by-step) []
11) Once rated -> return to product.html and display showNotification(product_review_added) []
12) Find a way to limit it to one review per user per product without logging in. []
13) Logged in != -> annon review default profile picture and name []
14) Admin page to upload products via frontend instead of db injections []
15) Find a way to scan product QR codes and trigger the /product to load []
16) If I scan a bag of chips it should trigger the chips page []
17) Store QR code value in DB and have a mapper search QR codes on request []
18) Fix website for IPad & Phone sizes []
19) Beta Release []
20) Test features, bugs and deployment []
21) Alpha Release []

################################################
             VERSION 2.0.0 RELEASE
################################################

1) Favorite products []
2) Wish list []
3) Shopping list []
4) Profile.html with relevant information []
5) Make it like a game. 1 review -> XP []
6) XP triggers badges and colors maybe []